# PetTester

Evaluates the trained Cat vs Dog classifier.

## Usage
1.  Ensure you have a trained model in `../shared/models`.
2.  Run the tester:
    ```bash
    python main.py
    ```
    It will automatically pick the latest model and the test data from `../shared/data/composed/test`.